version: 2

sources:
  - name: bikes                   
    database: citi-bike-analytics     
    schema: raw                    
    description: "Raw bike trip data ingested from S3"
    tables:
      - name: trips_test              
        description: "One row per bike trip"
      - name: citi_bike_raw              
        description: "One row per bike trip"
